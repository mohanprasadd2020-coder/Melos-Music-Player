services:
  - type: web
    name: jiosaavn-jam
    runtime: node
    buildCommand: bun install && bun run build
    startCommand: bunx serve -s dist -l 3000
    envVars:
      - key: NODE_ENV
        value: production
